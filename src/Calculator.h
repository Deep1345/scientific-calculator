#ifndef CALCULATOR_H
#define CALCULATOR_H

class Calculator {
public:
    double squareRoot(double x) const;
    unsigned long long factorial(int x) const;
    double naturalLog(double x) const;
    double power(double base, double exponent) const;
};

#endif